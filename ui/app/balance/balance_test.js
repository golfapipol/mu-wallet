'use strict';

describe('myApp.balance module', function() {

  beforeEach(module('myApp.balance'));

  describe('balance controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var BalanceCtrl = $controller('BalanceCtrl');
      expect(BalanceCtrl).toBeDefined();
    }));

  });
});