
'use strict';

describe('myApp.transfer module', function() {

  beforeEach(module('myApp.transfer'));

  describe('transfer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TransferCtrl = $controller('TransferCtrl');
      expect(TransferCtrl).toBeDefined();
    }));

  });
});